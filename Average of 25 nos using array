#include <stdio.h>
int main() {
    float arr[25], sum = 0;
    printf("Enter 25 numbers:\n");
    for(int i = 0; i < 25; i++) {
        scanf("%f", &arr[i]);
        sum += arr[i];
    }
    printf("Average: %.2f", sum / 25);
    return 0;
}
