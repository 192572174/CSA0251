#include<stdio.h>
int main(){
    int num, temp, digit, sum=0;
    scanf("%d", &num);
    temp=num;
    while(temp>0){
        digit=temp%10;
        sum=sum+digit;
        temp=temp/10;
    }
    if(numm%sum==0){
        printf("Harshad no");
    }
    else{
        printf("Not Harshad no");
    }
    return 0;
}
