#include<stdio.h>
void main(){
    int num, rev=0, rem, original;
    scanf("%d", &num);
    original=num;
    while(num!=0)
    {
        rem=num%10;
        rev=rev*10+rem;
        num/=10;
    }
    if(original==rev)
        printf("Palindrome");
    else
        printf("Not palindrome");
}
