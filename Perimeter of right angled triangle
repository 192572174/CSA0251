#include <stdio.h>
#include <math.h>
int main() {
    double a, b;
    printf("Enter base and height: ");
    scanf("%lf %lf", &a, &b);
    double perimeter = a + b + sqrt(a*a + b*b);
    printf("Perimeter: %.2f\n", perimeter);
    return 0;
}
